import 'package:flutter/material.dart';

class ProductCreateTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(child: Text('create a product'));
  }
}
